#!/usr/bin/env bash
# Usage: script/build
#
# Builds jqplay

set -e

npm install
cd server
rice embed-go
cd ..
go build -ldflags="-X 'main.GinMode=release'" -o ./bin/jqplay ./cmd/jqplay
echo "built bin/jqplay"
